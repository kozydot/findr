specVersion: "v1"
serviceId: "price-comparator"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "price-comparator-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
